import { BaseTemplateWithHooksProps } from ".";

// "*.mocks" are sample data for your component's properties.

const base: BaseTemplateWithHooksProps = {
  children: null,
};

const mockBaseTemplateWithHooksProps = {
  base,
};

export default mockBaseTemplateWithHooksProps;
